# EchoCare v0.1 Demo Freeze

This folder is a static snapshot created before the Node backend proxy migration.

Contents:
- Static PWA files: `index.html`, `styles.css`, `app.js`, `sw.js`, `manifest.webmanifest`, `icon.svg`
- Local analysis module: `src/analysis.js`
- Original tests and package metadata

Use this folder when you need to recover or demonstrate the pure frontend version.
The main project root now contains the backend proxy version.
